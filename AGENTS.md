# stage.acttub.com — 이 대사, 어느 무대? (가칭)

대사 한 줄만 보고 작품을 맞히는 열 문제짜리 퀴즈. acttub 유입용 서브프로젝트.

**서버가 없다. 런타임 LLM이 없다.** 정적 파일 한 벌이고, 상태는 전부 브라우저 안에 있다.

## 이 저장소를 고칠 때

- **화면에 보이는 문장은 `copy.js`에만 있다.** 다른 파일에 한국어 문장을 쓰지 않는다.
  예외는 둘뿐이다 — `data.js`의 대사·작품명·장면 설명·질문(문항 데이터라 거기 있어야 한다),
  그리고 `index.html`의 `<title>`·`description`·OG 문구(화면이 아니라 검색 결과와 공유
  미리보기에 나오는 것이라 정적 HTML에 있어야 한다). **이 둘도 사용자가 읽는 글이라
  아래 "연기를 평가하지 않는다"가 똑같이 적용된다.**
- **문구나 문항을 고쳤으면 `build-fonts.py`를 다시 돌린다.** 안 돌리면 새 글자가 서브셋에 없어
  시스템 폰트로 렌더된다. 대사가 늘어나는 프로젝트라 이걸 잊기 쉽다.
- **스타일을 고쳤으면 `npm run css`를 돌린다.** `styles.css`는 생성물이라 직접 고치지 않는다.
- 색·모서리·글자 크기는 `src/brand/tailwind-starter.css`의 토큰만 쓴다. 새 값을 지어내지 않는다.
  `src/brand/`는 내부 디자인 정본에서 생성돼 들어온 파일이라 **여기서 고치지 않는다.**

## 문항을 추가할 때 — 저작권이 먼저다

- **보호기간이 끝난 작품만 넣는다.** 지금 실린 건 셰익스피어·그리스 비극·체호프·입센·
  몰리에르·괴테·고골·뷔히너·스트린드베리·와일드다.
- **한국어 대사를 시중 번역본에서 가져오지 않는다.** 원작이 자유로워도 번역문에는 번역자의
  저작권이 따로 붙는다. 여기 있는 문장은 전부 이 저장소에서 새로 옮긴 것이다.
- 현대극은 보호기간이 남아 빼 두었다. 넣고 싶으면 인용 범위 판단이 먼저다 — 재미를 이유로
  그냥 넣지 않는다.
- `decoys`(오답 보기)는 아무 작품이나 넣지 않는다. 같은 작가나 같은 결의 작품을 넣어야
  대사를 읽고 고르는 문제가 되고, 아니면 작가 이름만 보고 찍는 문제가 된다.

## 연기를 평가하지 않는다 — 이 서비스의 존재 조건

맞히는 건 **작품 이름**이다. 사용자의 연기가 아니다.

- 잘한다/못한다·재능·소질·실력·연기력에 대한 말을 쓰지 않는다.
- 결과의 이름표는 **극을 얼마나 봤나**에 대한 것이다. 사람을 서술하지 않는다.
  낮은 구간에도 모자라다는 말을 쓰지 않는다.
- `data.js`의 `ask`는 **답을 주지 않는 질문**이다. "이렇게 하세요"가 되는 순간 교정이 된다.
  기법(발성·발음·감정)을 답으로 흘리는 문장도 쓰지 않는다.
- `level`(easy/mid/hard)은 난이도 배분용 내부값이다. 화면에 노출하지 않는다.

## 배포

- Vercel git 연동. **`main`에 푸시하면 곧바로 실서비스가 바뀐다.**
- 빌드 명령이 없다 — `styles.css`와 `fonts/`는 커밋된 생성물이다. 빌드를 잊으면 옛 스타일이 배포된다.
- `/result/*`는 `vercel.json`에서 `index.html`로 넘긴다. **catch-all 폴백을 쓰지 않는다** —
  그러면 `/_vercel/insights/*`까지 삼켜 방문 계측이 조용히 죽는다.
- 결과 주소로 직접 들어와도 남의 결과를 보여주지 않는다. 본인이 풀어야 결과가 나온다.

## 공개 저장소다

비밀값·실명·내부 문서 경로·내부 티켓 링크를 커밋하지 않는다.
